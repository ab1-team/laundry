name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          command_timeout: 15m
          script: |
            set -e
            cd /home/enpii/apps/laundryaja/backend

            git pull origin main

            # .env di-manage di dalam container oleh docker/entrypoint.sh
            # — copy dari .env.example, chown www-data. Tidak perlu
            # bootstrap atau chown/chmod di host lagi. Sebelumnya workflow
            # bind-mount .env dari host ke container tapi menyebabkan
            # "Permission denied" / "Operation not permitted" di artisan
            # key:generate karena owner host ≠ container user.

            # Build image. Tanpa --no-cache supaya Docker pakai layer cache
            # dari build sebelumnya — second build hanya ~30 detik karena
            # cuma re-build layer yang berubah. cold build (~2-3 menit)
            # hanya di first deploy atau kalau Dockerfile/composer.lock
            # berubah.
            echo "Building image..."
            docker compose build app

            docker compose down --remove-orphans
            docker compose up -d

            # Tunggu container app siap.
            echo "Waiting for app container..."
            for i in $(seq 1 30); do
              if docker compose exec -T app php -r 'exit(0);' >/dev/null 2>&1; then
                break
              fi
              sleep 2
            done

            # Generate APP_KEY kalau masih kosong. Cek dari host .env
            # (env_file inject host file sebagai env vars container —
            # sama isinya dengan .env di dalam container). Kalau host
            # .env ada, key di sana sudah persistent dan key:generate
            # di container akan overwrite sama nilainya — skip kalau
            # sudah ada.
            CURRENT_KEY=$(grep '^APP_KEY=' .env | cut -d= -f2-)
            if [ -z "$CURRENT_KEY" ]; then
              echo "APP_KEY kosong — generate..."
              docker compose exec -T app php artisan key:generate --force
            fi

            # Run migrations SEBELUM cache:clear — kalau driver cache/session
            # pakai database, tabel 'cache' dan 'sessions' harus exist dulu.
            echo "Running migrations..."
            docker compose exec -T app php artisan migrate --force || {
              echo "::warning::Migration gagal. Cek docker compose logs app untuk detail."
              docker compose logs --tail=100 app
            }

            # Storage symlink (idempotent).
            docker compose exec -T app php artisan storage:link || true

            # Clear config & view cache supaya .env baru terbaca.
            docker compose exec -T app php artisan config:clear || true
            docker compose exec -T app php artisan cache:clear || true

            # Optimize caches.
            docker compose exec -T app php artisan optimize || true

            docker compose ps
            docker compose logs --tail=50 app