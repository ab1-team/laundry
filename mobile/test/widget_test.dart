// Placeholder test — widget tests akan ditambah setelah masing-masing screen
// distabilkan. Saat ini hanya smoke test sederhana.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
